## Gádor JS Footer

> This should be used in the following manner

Install with the command:

```
npm install --save gador-footer-js
```

Add to a JavaScript Project with the following code:

```javascript
import { footer } from 'gador-footer-js';

footer('Some Name');
```